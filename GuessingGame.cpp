#include <iostream>
#include <stdlib.h>
#include <time.h>

using namespace std;

int main()
{
  int secretNum, guessNum;
  guessNum = -1;
  srand (time(NULL));
  
  cout << "Guess the correct number between 0-100.";
  while (guessNum != secretNum){
    
    cin >> input;
  }
  return 0;
}
